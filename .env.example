# PostgreSQL
# Usada por packages/database (Prisma)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/energy_bot?schema=public"

# IA (Anthropic / Claude)
# Se usa en packages/services/src/receipt/receipt.service.ts (leer el recibo)
# y packages/services/src/plan/plan.service.ts (generar el plan).
# Sacala de https://console.anthropic.com/settings/keys
AI_PROVIDER_API_KEY=

# Modelo a usar. Por defecto claude-haiku-4-5, el mas economico con vision
# (USD 1 por millon de tokens de entrada, 5 de salida). Para mas precision
# leyendo el recibo: claude-sonnet-5 o claude-opus-5.
AI_MODEL=claude-haiku-4-5

# Bot / Baileys
# Nombre de la sesion de Baileys (se guarda el estado de auth en una carpeta local con este nombre)
BOT_SESSION_NAME=energy-bot
